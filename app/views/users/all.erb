<h2>Users</h2>


<table>
  <tr>
    <th>name</th>
    <th>email</th>
  </tr>
  <% User.all.each do |user|  %>

  <tr>
    <td><%= link_to user.first_name + " " + user.last_name, user_path(user) %></td>
    <td><%= user.email %></td>

  </tr>
  <% end %>

</table>
